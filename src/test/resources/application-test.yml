spring:
  r2dbc:
    url: r2dbc:postgresql://localhost:5432/testdb
    username: testuser
    password: testpass
    pool:
      max-size: 20

  flyway:
    enabled: true
    locations:
      - classpath:db/migration/schema
      - classpath:db/migration/test